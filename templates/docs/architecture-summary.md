# Architecture Summary

Concise technical context, per `INITIATION.md` §3 — only when applicable. Skip or keep this to a few lines if the project is simple; do not manufacture architecture documentation the project doesn't need.

## Context

What this system is and its major components/services, in a few sentences.

## Key decisions in effect

Pointers to entries in `docs/decisions.md`, not duplicated content.

## Constraints

Anything a Builder or Verifier must know before touching this system (data model invariants, external contracts, deployment topology) that isn't obvious from reading the code.

_Detailed architecture/ADRs, if this project needs them, live under `architecture/` per `INITIATION.md` §3's optional directories — create that only when the repository or approved product work actually needs it._
